name: ThreeTierApp

location:
  docker:
    endpoint: http://localhost:2375
    identity: /home/travis/cert.pem
    credential: /home/travis/key.pem
    imageId: "cloudsoft/centos:7"
    loginUser.password: p4ssw0rd

services:
  - type: server
